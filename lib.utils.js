const MIME_JSON = 'application/json';
const MIME_PDF = 'application/pdf';
const MIME_URL_ENCODED = 'application/x-www-urlencoded';
const MIME_FORM_URL_ENCODED = 'application/x-www-form-urlencoded';
const MIME_MULTIPART_FORM_DATA = 'multipart/form-data';
const MIME_JPEG_IMAGE = 'image/jpeg';
const MIME_PNG_IMAGE = 'image/png';
const PDF_FILENAME = 'players.pdf';
const DEFAULT_ORDER_BY = 'id';
const ERR_404_MESSAGE = 'Sorry, this path seems to be missing !';
const ERR_422_MESSAGE = 'Sorry, we seems to be unable to process the datas !';

module.exports = {
    MIME_JSON,
    MIME_PDF,
    MIME_URL_ENCODED,
    MIME_FORM_URL_ENCODED,
    MIME_MULTIPART_FORM_DATA,
    MIME_JPEG_IMAGE,
    MIME_PNG_IMAGE,
    PDF_FILENAME,
    DEFAULT_ORDER_BY,
    ERR_404_MESSAGE,
    ERR_422_MESSAGE
}